{
  "name": "dravr-messaging-eval-botium",
  "version": "0.1.0",
  "private": true,
  "description": "Botium conversational scenarios for real-API messaging-eval runs. Scaffold — activation requires the second Slack test-driver app (see ../.github/workflows/messaging-eval.yml Phase 2 activation checklist). Run with: bun run scenario:slack",
  "scripts": {
    "scenario:slack": "botium-cli run --convos convo/ --asserter asserter/ --config botium.json",
    "scenarios:all": "botium-cli run --convos convo/ --asserter asserter/"
  },
  "devDependencies": {
    "botium-cli": "^1.15.0",
    "botium-connector-slack": "^0.1.0",
    "botium-core": "^1.13.0"
  }
}
