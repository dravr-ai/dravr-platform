// ABOUTME: Stand-in for stylesheet imports under jest — NativeWind compiles global.css at build time, not in tests
// ABOUTME: Mapped from every `.css` specifier by jest.config.js so the root layout can render in a test

module.exports = {};
