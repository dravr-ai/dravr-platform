# ABOUTME: Test opening conversation history from chat
# ABOUTME: Converted from chat.test.js "should open navigation drawer" - tests history-button

appId: com.pierre.fitness

---
# Launch and login
- runFlow:
    file: ../helpers/launch-app.yaml
- runFlow:
    file: ../helpers/login.yaml

# Test: should open conversation history via history button
- tapOn:
    id: "history-button"

# Should see conversations screen
- extendedWaitUntil:
    visible:
      text: "Conversations"
    timeout: 3000
