# ABOUTME: Test send button enables when text is entered
# ABOUTME: Converted from chat.test.js "should enable send button when text is entered"

appId: ai.dravr.app

---
# Launch and login
- runFlow:
    file: ../helpers/launch-app.yaml
- runFlow:
    file: ../helpers/login.yaml

# Start fresh conversation
- runFlow:
    file: ../helpers/new-chat.yaml

# Type text in message input
- runFlow:
    file: ../helpers/focus-message-input.yaml
- inputText: "Hello Pierre"

# Send button testID switches to send-button when canSend is true.
# Use extendedWaitUntil to allow React state to propagate.
- extendedWaitUntil:
    visible:
      id: "send-button"
    timeout: 5000
