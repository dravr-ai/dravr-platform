# ABOUTME: Test that email and password input fields are visible
# ABOUTME: Converted from login.test.js "should show email and password inputs"

appId: com.pierre.fitness

---
# Launch app fresh
- runFlow:
    file: ../helpers/launch-app.yaml

# Verify email input is visible
- assertVisible:
    id: "email-input"

# Verify password input is visible
- assertVisible:
    id: "password-input"
