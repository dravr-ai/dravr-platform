# ABOUTME: Visual tests for settings tab
# ABOUTME: Converted from mobile-visual.test.js "Settings Tab" tests

appId: com.pierre.fitness

---
# Launch and login
- runFlow:
    file: ../helpers/launch-app.yaml
- runFlow:
    file: ../helpers/login.yaml

# Test: settings - tab icon visible and tappable
- runFlow:
    when:
      visible:
        id: "tab-settings"
    commands:
      - tapOn:
          id: "tab-settings"

# If no tab, navigate via bottom tab bar "Settings"
- runFlow:
    when:
      notVisible:
        id: "tab-settings"
    commands:
      - tapOn: "Settings"

# Test: settings - displays screen
- extendedWaitUntil:
    visible:
      id: "settings-screen"
    timeout: 5000

# Test: settings - displays profile section (use testID for reliable matching)
- extendedWaitUntil:
    visible:
      id: "settings-profile-section"
    timeout: 10000

# Test: settings - displays connections section (may need scroll)
- runFlow:
    when:
      visible: "Connections"
    commands:
      - assertVisible: "Connections"

- runFlow:
    when:
      visible: "Providers"
    commands:
      - assertVisible: "Providers"
