# ABOUTME: Test complete coach editor flow to create a coach
# ABOUTME: Fills all fields in the single-page editor and saves

appId: com.pierre.fitness

---
# Launch and login
- runFlow:
    file: ../helpers/launch-app.yaml
- runFlow:
    file: ../helpers/login.yaml

# Navigate to coach library
- runFlow:
    file: ../helpers/navigate-to-coaches.yaml

# Open editor
- tapOn:
    id: "create-coach-button"
- extendedWaitUntil:
    visible:
      id: "coach-editor-screen"
    timeout: 5000

# Fill title
- tapOn:
    id: "coach-title-input"
- inputText: "E2E Editor Coach"

# Select category
- tapOn:
    id: "category-picker"
- extendedWaitUntil:
    visible:
      text: "Training"
    timeout: 3000
- tapOn: "Training"

# Fill description
- tapOn:
    id: "coach-description-input"
- inputText: "Created via E2E test"

# Dismiss keyboard. hideKeyboard fails in Expo Go v55.
- tapOn:
    point: "50%,20%"

# Scroll to system prompt (expanded by default)
- scrollUntilVisible:
    element:
      id: "system-prompt-input"
    direction: DOWN
    timeout: 5000

# Fill system prompt
- tapOn:
    id: "system-prompt-input"
- inputText: "You are an E2E test coach created by Maestro."

# Dismiss keyboard
- tapOn:
    point: "50%,20%"

# Scroll to tags section and expand it
- scrollUntilVisible:
    element:
      id: "tags-section"
    direction: DOWN
    timeout: 5000
- tapOn:
    id: "tags-section-toggle"
- extendedWaitUntil:
    visible:
      id: "tag-input"
    timeout: 3000

# Add a tag
- tapOn:
    id: "tag-input"
- inputText: "e2e"
- tapOn:
    id: "add-tag-button"

# Dismiss keyboard
- tapOn:
    point: "50%,20%"

# Save the coach
- tapOn:
    id: "save-button"

# Should return to library
- extendedWaitUntil:
    visible:
      id: "coach-library-screen"
    timeout: 10000

# Verify coach appears in list
- scrollUntilVisible:
    element:
      text: "E2E Editor Coach"
    direction: DOWN
    timeout: 5000
- assertVisible: "E2E Editor Coach"
