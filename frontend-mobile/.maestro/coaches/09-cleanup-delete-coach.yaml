# ABOUTME: Cleanup - delete the test coach created in test 06
# ABOUTME: Navigates to coach detail screen and uses the delete button

appId: com.pierre.fitness

---
# Launch and login
- runFlow:
    file: ../helpers/launch-app.yaml
- runFlow:
    file: ../helpers/login.yaml

# Navigate to coach library
- runFlow:
    file: ../helpers/navigate-to-coaches.yaml

# Filter to user-created coaches for faster FlashList rendering
- tapOn:
    id: "source-filter-user"

# Scroll to find the test coach. If it doesn't exist (test 06 failed), skip.
- scrollUntilVisible:
    element:
      text: "E2E Test Coach"
    direction: DOWN
    timeout: 30000

# Tap to navigate to the detail screen. This is more reliable than
# long-press on FlashList items which can fail due to cell recycling.
- tapOn: "E2E Test Coach"

# Wait for the detail screen to load
- extendedWaitUntil:
    visible:
      id: "coach-detail-screen"
    timeout: 15000

# Tap the delete button on the detail screen
- tapOn:
    id: "delete-button"

# Confirm deletion in the alert dialog
- extendedWaitUntil:
    visible: "Delete"
    timeout: 5000
- tapOn: "Delete"

# Wait to return to library after deletion
- extendedWaitUntil:
    visible:
      id: "coach-library-screen"
    timeout: 15000

# Verify coach is deleted
- extendedWaitUntil:
    notVisible: "E2E Test Coach"
    timeout: 10000

# Reset filter
- tapOn:
    id: "source-filter-all"
