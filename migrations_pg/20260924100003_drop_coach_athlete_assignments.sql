-- ABOUTME: Drops coach_athlete_assignments: the /api/roster junction no client ever called (PostgreSQL version)
-- ABOUTME: A coach's roster is the live members of the groups they coach; the table's indexes go with it

DROP TABLE IF EXISTS coach_athlete_assignments;
